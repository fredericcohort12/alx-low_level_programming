#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: the int to check
 * Retur: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	return (-n);
}
